/**
 * 1.Create firebase Project
 * 2. create web app
 * 3. npm i firebase
 * 4. save firebase config and export default app
 * 5. Build > Authentication > enable sign in method
 * 6. create sign up and sign in route
*/

/**
 * 1. Create a context provider file
 * 2.Create a context and set provider
 * 3.set the children props
 * 4. Set context value
 * 5. set the provider
*/

/**
 * ------------------Hosting--------------
 * 1. 1 time per pc(only ekbr krte hbe)
 * 2. npm install -g firebase-tools
 * 3.firebase login
 * 
 * ------------------forEach project one time------------------
 * 1. firebase init
 * 2. proceed
 * 3. hosting : firebase(up and down arrow) use spaceBar to select
 * 4. existing project and select the project carefully
 * 5. which project as public directory: dist (for vite)
 * 6. single page application: yes
 * 7. continuous deployment: no
*/

/**
 * ---------for every time deploy-------------
 * 1. npm run build
 * 2. firebase deploy
*/